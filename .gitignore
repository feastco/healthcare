# Environment & secrets — TASK-0001 / ADR-012: .env must never be tracked
.env
.env.*
!.env.example

# Source-of-truth documents stay local — not part of the git-tracked app codebase
docs/

# Global AI-agent conventions & skills stay local
AGENTS.md
agents/
agents/skills/